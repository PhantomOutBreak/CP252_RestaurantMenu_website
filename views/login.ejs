<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ระบบยืนยันตัวตน</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(to right, #000428, #004e92);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .login-container {
      background-color: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      text-align: center;
      width: 350px;
    }
    .login-container h1 {
      margin-bottom: 2px;
      font-size: 25px;
      font-weight: bold;
    }
    .login-container input {
      width: 94%;
      padding: 10px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 16px;
    }
    .login-container button {
      width: 100%;
      padding: 15px;
      border: none;
      border-radius: 5px;
      color: white;
      font-size: 20px;
      font-weight: bold;
      cursor: pointer;
    }
    .login-btn {
      background: linear-gradient(to right, #16a416, #54ff44);
      margin-bottom: 10px;
    }
    .register-btn {
      background: linear-gradient(to right, #0000FF, #0dcfff);
    }
  </style>
</head>
<body>
  <div class="login-container">
    <% if (error) { %>
      <div style="color: red; margin-top: 10px;">
          <%= error %>
      </div>
    <% } %>
    <% if (success) { %>
      <div style="color: green; margin-top: 10px;">
          <%= success %>
      </div>
    <% } %>
    
    <h1>ระบบยืนยันตัวตนเพื่อเข้าสู่ระบบ</h1>
    <form action="/login" method="POST">
      <input type="email" name="email" placeholder="Email" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit" class="login-btn">เข้าสู่ระบบ</button>
    </form>
    <a href="/register">
      <button type="button" class="register-btn">ลงทะเบียน</button>
    </a>
  </div>
</body>
</html>