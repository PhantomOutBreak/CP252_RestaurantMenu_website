<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>4 เมนูลับคุณย่า</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #4d4d4d;
            background-image: url('/image/bg-homepage.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh;
        }

        .topbar {
            width: 100%;
            height: 50px;
            background-color: #4d4d4d;
            position: absolute;
            top: 0;
            left: 0;
            z-index: 10;
        }

        .container {
            width: 100%;
            height: 100vh;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        .text-overlay {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 55px;
            font-weight: bold;
            color: white;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7);
            background-color: rgba(0, 0, 0, 0.5);
            padding: 10px 25px;
            border-radius: 8px;
        }

        .login-btn {
            padding: 10px 20px;
            background: linear-gradient(to right, #13EE1A, #afff04);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-family: Arial, Helvetica, sans-serif;
            font-weight: bold;
            transition: transform 0.5s ease, box-shadow 0.5s ease;
            position: relative; 
            transform-origin: top right; /* กำหนดจุดศูนย์กลางการขยาย */
        }

        .register-btn {
            padding: 10px 20px;
            background: linear-gradient(to right, #006AFF, #00f2ff);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-family: Arial, Helvetica, sans-serif;
            font-weight: bold;
            transition: transform 0.5s ease, box-shadow 0.5s ease;
            position: relative; 
            transform-origin: top right; /* กำหนดจุดศูนย์กลางการขยาย */
        }

        .register-btn:hover {
            transform: scale(1.2); 
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            color: rgb(126, 255, 255);
        }
        .login-btn:hover {
            transform: scale(1.2);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            color: rgb(0, 124, 35);
        }
    </style>
</head>

<body>
    <nav style="background: linear-gradient(to right, #4158D0,#C850C0,#FFCC70);"
    class="navbar navbar-expand-lg navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="menu.ejs">4 เมนูลับคุณย่า</a>
            <div class="d-flex">
                <a class="login-btn me-2" href="login">เข้าสู่ระบบ</a>
                <a class="register-btn" href="register">ลงทะเบียน</a>
            </div>
        </div>
    </nav>
    <div class="container">
        <div class="text-overlay">4 เมนูลับคุณย่า</div>
    </div>
</body>

</html>